name: Blog Post Workflow (Disabled)
on:
  workflow_dispatch:
jobs:
  placeholder:
    name: Blog workflow placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder step
        run: echo "Blog workflow disabled - no RSS feed configured. Enable when you have a blog!"
        
